{
  "buildCommand": "cd artifacts/blend-coffee && npm run vercel-install && npm run build",
  "outputDirectory": "artifacts/blend-coffee/dist/public",
  "installCommand": "cd artifacts/blend-coffee && npm run vercel-install",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
